/**
 * Внутренний модуль: не входит в "exports", поэтому снаружи недоступен.
 * Существует, чтобы проверять, что глубокий импорт @shared/lib/src/internal запрещён.
 */
export const internalOnly = 'not part of the public API';
