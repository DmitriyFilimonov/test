/**
 * Запрос «показать узел»: холст дерева доводит узел до экрана, таблица прокручивается к
 * строке. `nonce` растёт на каждой операции выбора, поэтому повторный выбор того же узла —
 * новый запрос, хотя `id` тот же.
 */
export interface RevealRequest {
  id: string;
  nonce: number;
}
