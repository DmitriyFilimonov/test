# Требования к пакетам-сущностям (@entities/org-tree)

Выявлены на ревью шага 01 (2026-09-15). Файл назван «step-02» по ожиданию того ревью;
фактически пакет сделан в завершающей части шага 01 — см. `docs/ai-log/2026-09-15-step-01-tree-render.md`.

- [x] **`isEqual` для оргдерева обязан замечать удаление и добавление узлов.** Сравнение
      только по максимальному `updatedAt` пропускает удаление (на моке
      `POST /api/dev/touch?mode=delete` даёт 53 → 52 узла при том же максимуме). Из двух
      вариантов — пара `[length, max(updatedAt)]` (не ловит «удалили один, добавили
      другой») и совпадение `id → updatedAt` (тоже O(n), ловит) — по решению пользователя
      реализован второй: `isSameOrgTree`. Опирается на контракт «любое изменение узла
      меняет его `updatedAt`». → `2026-09-15-step-01-tree-render.md`
- [x] Тест `isEqual` на три случая: без изменений (ссылка `data` та же), `mode=update`,
      `mode=delete` — `model/query.test.ts` (реальный стор, fetch подменён) и
      `model/isEqual.test.ts`. → `2026-09-15-step-01-tree-render.md`
- [x] Сага сущности зарегистрирована в `packageSagas` и работает под `supervise`.
      Ограничение остаётся: при перезапуске вотчера счётчик подписчиков обнуляется.
      → `2026-09-15-step-01-tree-render.md`
